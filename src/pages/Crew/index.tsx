import { PageType, PageWithBackground } from "../PageWithBackground";

export const Crew = () => {
    return (
        <PageWithBackground type={PageType.Crew}>
            Crew
        </PageWithBackground>
    );
}